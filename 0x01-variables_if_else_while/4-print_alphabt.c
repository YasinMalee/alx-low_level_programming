#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main print alphabet
 *
 * return: 0 (success)
 */

int main(void)
{
int ch;
ch = 'a';
while (ch <= 'z')
{
	if (ch = 'e')
	{
	putchar(++ch);
	++ch;
	}
	if (ch = 'q')
	{
		putchar(++ch);
		++ch;
	}
}

return (0);

}
